{
    "name": "mandelbrot-set",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "parcel": "^1.12.3",
        "prettier": "^1.18.2",
        "sass": "^1.22.12",
        "tslint": "^5.20.0",
        "typescript": "^3.6.3"
    },
    "scripts": {
        "prestart": "rm -rf dist",
        "start": "parcel public/index.html",
        "prebuild": "rm -rf dist",
        "build": "parcel build public/index.html"
    }
}
